<% provide(:title, 'All exams') %>
<h1>All exams</h1>

<%= button_to "New exam", exams_path, class: "btn btn-inverse btn-lg new_fact", method: :post %>

<%= will_paginate %>

<table class="table table-hover">
	<thead>
		<tr>
			<th>Exam#</th>
		   <th>User</th>
		   <th>Completed</th>
		   <th>Created on</th>
		   <th>Options</th>
		</tr>
	</thead>
	<tbody>

	<%= render @exams %>

  </tbody>
</table>

<%= will_paginate %>

<div><%= link_to "Back", exams_path, class: "btn btn-inverse btn-lg" %></div>


