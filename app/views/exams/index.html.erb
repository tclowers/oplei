<% provide(:title, 'All Exams') %>
<h1>All Exams</h1>

<%= button_to "New Exam", new_exam_path, class: "btn btn-inverse btn-large new_fact", method: :get %>

<%= will_paginate %>

<table class="table table-hover">
	<thead>
		<tr>
			<th>Exam#</th>
		   <th>User</th>
		   <th>Completed</th>
		   <th>Created on</th>
		   <th>Options</th>
		</tr>
	</thead>
	<tbody>

	<%= render @exams %>

  </tbody>
</table>

<%= will_paginate %>


